# Manual action gate

No manual upload is required yet.

The next manual action will only be requested after the app's package loader is verified against the folder catalog on `refactor/dynamic-folder-catalog`.
